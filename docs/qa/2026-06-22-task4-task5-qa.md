# WorthIT — QA Report
**Date:** 2026-06-22  
**Scope:** Task 4 (Product Deduplication) `5c855ba` + Task 5 (Fraud Detection) `949028c`  
**Reviewed by:** Claude (Senior QA Engineer role)  
**Tests after fixes:** 126 passing, 0 failing (+33 new tests)

---

## Issues Found & Fixed

### Critical

| # | File | Issue | Fix |
|---|------|--------|-----|
| C1 | `productService.ts` | `$push` to `analysisHistory` with no size cap — document grows unbounded toward 16MB MongoDB limit (same issue QA removed in Task 2, re-introduced in lighter form) | Added `$each/$slice: -50` — keeps last 50 entries only |
| C2 | `productService.ts:12` | `canonicalUrl = listing.url \|\| listing.title` — when no URL, any two products sharing a title ("iPhone 13") merge into the same Product document | Skip product creation when no URL (`if (!listing.url) return null`); strip query params from URL |
| C3 | `analysis.route.ts` | Two separate `analysisId` UUIDs generated per request — `run.ts` generates one (stored in cache), route generates another (stored in DB). On cache hit, client receives ID "A" which doesn't exist in DB → `GET /analysis/A` returns 404 | Removed second `buildAnalysisId()` call; route now uses `result.analysisId` throughout |

### Medium

| # | File | Issue | Fix |
|---|------|--------|-----|
| M1 | `productService.ts` | `$inc: { analysisCount: 1 }` missing from `updateProductAnalysisHistory` — counter never incremented | Added to the update operation |
| M2 | `fraudDetection.ts` | `guessCategory` used `category.slice(0,4)` prefix matching — `'car'.slice(0,4) = 'car'` matched `carpet`, `cartoon`, `scar`, etc. triggering false "unusually low price for Car" warnings on irrelevant listings | Replaced with explicit keyword arrays per category; `car` uses `\bcar\b`-style detection via list with spaces |
| M3 | `fraudDetection.ts` | `detectMissingAccessories` used its own `fullText.includes(name)` for category detection — "MacBook" never matched `laptop` | Replaced with `guessCategory()` call — consistent with the rest of the module |

### Minor

| # | Fix |
|---|-----|
| m1 | No tests for `fraudDetection.ts` (5 pure functions) or `productService.ts` | Added 33 tests across 2 new test files |
| m2 | `analysis.route.ts` had two separate `import` lines from same `analysisRepository.js` | Merged into one |

---

## New Tests Added

**`tests/fraudDetection.test.ts`** — 26 tests:
- `guessCategory`: iphone→phone, macbook→laptop, ps5→gaming, carpet≠car, cartoon≠car, scar≠car
- `detectPriceSanity`: low/high/normal prices, unrecognised category, carpet-not-car
- `detectStockPhoto`: unsplash/shutterstock flagged, facebook URL clean, no image
- `detectUrgencyLanguage`: 2+ keywords fires, single keyword doesn't, clean listing
- `detectMissingAccessories`: phone missing accessories, MacBook correctly identified via guessCategory, clean listing, unknown category
- `getAllRedFlags`: clean listing, multiple concurrent flags

**`tests/productService.test.ts`** — 7 tests:
- No URL → returns null, no DB call
- Query params stripped before deduplication
- Returns productId on success
- DB not ready → returns null
- `updateProductAnalysisHistory` includes `$slice: -50` and `$inc: analysisCount`

---

## Carry-Forward Open Items

- [ ] `detectUrgencyLanguage` single-keyword threshold — one "urgent" never fires; intentional but undocumented
- [ ] Hebrew urgency phrases not covered (e.g. `דחוף`, `חייב למכור`) — already in specsExtractor but not in fraudDetection
- [ ] Add `ref: 'User'` to `Analysis.userId` for Mongoose populate
- [ ] `Product.specs.year` typed `Number` but specsExtractor returns string

---

## Verdict

**✅ Ready for Production** (Tasks 4 + 5 scope)

Product deduplication is now safe (URL-only, query-param-stripped, capped history). Fraud detection category matching is correct with no false positives on car/carpet. Single `analysisId` used consistently throughout the request lifecycle. 126 tests passing.

**→ Task 6 approved to proceed.**
